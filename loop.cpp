#include <iostream>
using namespace std;

int main(){
    //operators
    // logical operators
    // && - and ( both conditions must be true. 1 < 5 && 5 < 10)
    // || - or (either condition must be true. 1 < 5 || 5 < 10)
    // ! - not (1 < 5) is false, but !(1 < 5) is true

    // compersition operators
    // == - equal to
    // != - not equal to
    // > - greater than
    // < - less than
    // >= - greater than or equal to
    // <= - less than or equal to

    // loop
    // int i = 0;
    // for(int i = 0; i < 10; i++){
    //     for(int j = 0; j<i; j++){
    //         cout << "* ";
    //     }
    //     cout<< endl;
    // }
    // cout << !(1 < 5) << endl;
   /*int x = 5;
    int y = 10;
    double z = x/y;
    cout << z << endl; // 0 */




    
    return 0;
    
}